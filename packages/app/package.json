{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild --bundle ./index.ts",
    "show-config": "tsc --showConfig"
  },
  "devDependencies": {
    "esbuild": "^0.20.1",
    "tsconfigs": "workspace:^",
    "typescript": "^5.2.2"
  }
}
